<!DOCTYPE html>
<html lang="en">

<%= render 'layouts/head' %>
  <body>
<%= render 'layouts/navigation' %>
 <%= render 'layouts/header' %>


    <!-- Main Content -->
    <div class="container">
      <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
          <div class="post-preview">
           <!--  <a href="post.html">
              <h2 class="post-title">
                Man must explore, and this is exploration at its greatest
              </h2>
              <h3 class="post-subtitle">
                Problems look mighty small from 150 miles up
              </h3>
            </a> -->
              <% @blogpost.each do |blogpost| %>

                  <h2 class="section-heading"><%= blogpost.title%></h2>
                  <p style="font-style: italic; font-size: 15px"><%= blogpost.summary%></p>
                  <p><%= blogpost.content%></p>
                   <div class="clearfix">
                    <%=link_to 'Edit this post', edit_user_blogpost_path(blogpost.user, blogpost), class: 'btn btn-primary float-left' %>

                    <%= button_to "Delete", [blogpost.user,blogpost], class: 'btn btn-delete float-right', method: :delete, data: {confirm: "Really delete the post?"}
%>

              </div>
              <hr>
          <!-- Pager -->
           <div class="clearfix">
            <%= link_to 'Add Post', new_user_blogpost_path(blogpost.user), class: 'btn btn-primary float-right' %>
           <% end %>
         </div>
        </div>
      </div>
    </div>

    <hr>
<%= render 'layouts/footer' %>
    <!-- Bootstrap core JavaScript -->
    <script src="vendor/jquery/jquery.min.js"></script>
    <script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

    <!-- Custom scripts for this template -->
    <script src="js/clean-blog.min.js"></script>

  </body>

</html>
